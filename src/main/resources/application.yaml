spring:
  application:
    name: door-lock-api

  datasource:
    url: jdbc:mysql://localhost:3306/doorlock_db?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: doorlock_app
    password: 5805
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration


server:
  port: 8081